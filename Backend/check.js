const mysql = require('mysql2/promise'); require('dotenv').config(); (async () => { try { const pool = mysql.createPool({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME, port: Number(process.env.DB_PORT) }); const [rows] = await pool.query('SELECT * FROM access_code WHERE code = \'BHXKS8E5\''); console.log('Access code:', rows[0]); if (rows[0]) { const [pels] = await pool.query('SELECT * FROM pelajar WHERE id_access_code = ?', [rows[0].id]); console.log('Pelajar:', pels[0]); if (pels[0]) { const [hasils] = await pool.query('SELECT * FROM hasil_survey WHERE id_pelajar = ?', [pels[0].id]); console.log('Hasil:', hasils[0]); } } process.exit(0); } catch(e) { console.error(e); process.exit(1); } })();
